任务 5: 股票交易数据爬虫
20260115

学习使用 vscode, 写一个 Python / Typescript 交易数据爬虫, 存储到 supabase 里
- https://tushare.pro/
- http://baostock.com/mainContent?file=goodArticle.md

1. 通过 vscode / gemini 查资料, 在本地安装 uv (python 相关) / nodejs (typescript 相关) 运行环境
2. 通过 vscode agent, 写调用 baostock 来爬取日 K 线交易数据 -> 存储到 supabase 里的相关代码 (supabase 的建表也由 agent 完成), 并运行成功
3. 通过 vscode agent, 写调用 tushare 来爬取日 K 线交易数据 -> 存储到 supabase 里的相关代码 (supabase 的建表也由 agent 完成), 并运行成功

注: tushare 需要注册账户并获取积分, 有必要时需要 去闲鱼买一个

问题1
![[问题1.png]]